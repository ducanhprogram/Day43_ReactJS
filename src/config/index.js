const config = {
    routes: {
        home: "/",
        register: "/register",
        login: "/login",
        products: "/products",
        productDetail: "/products/:slug",
        users: "/users",
        notFound: "*",
    },
};

export default config;
