const config = {
    routes: {
        home: "/",
        register: "/register",
        login: "/login",
        login2: "/login2",
        products: "/products",
        productDetail: "/products/:slug",
        users: "/users",
        register2: "/register2",
        notFound: "*",
    },
};

export default config;
